# Assembly

Reserved for future knowledge-base assembly helpers (e.g. snippet selection by query). The chat-only runtime does not load anything from this folder — keyword-based snippet injection happens directly in `main.js`.
