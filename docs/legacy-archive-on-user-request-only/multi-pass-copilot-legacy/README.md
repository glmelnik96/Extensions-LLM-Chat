# Multi-pass Copilot (legacy)

Documents for the **historical** expression pipeline: multi-pass cloud calls, **latestExtractedExpression**, manual **Apply Expression**, and KB grounding **per pipeline stage**. The shipping panel uses the **AE Motion Agent** (tool loop) instead.

See the parent [../README.md](../README.md) before opening files here.
