{
  "name": "ae-motion-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Test harness for the pure functions of the AE Motion Agent CEP panel. The panel itself ships as plain browser scripts with no build step.",
  "scripts": {
    "test": "node --test"
  }
}
