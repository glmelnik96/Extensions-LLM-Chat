# Local overrides and secrets — never commit real keys
config/runtime-config.js
config/secrets.local.js

# Environment files (optional local tooling)
.env
.env.local
.env.*.local

# CEP developer debug (opens a local port; do not ship in public repos)
.debug

# OS
.DS_Store
Thumbs.db

# Editor / temp
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Node (if a package.json is added later)
node_modules/
